<Window x:Class="WPF_Usage.ExampleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Usage"
        mc:Ignorable="d"
        Title="Names" Height="240" Width="360">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label>Output</Label>
        <ListBox Grid.Row="1" x:Name="lstNames" />
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <Label>First URL:</Label>
            <TextBox x:Name="txtName" />
            <Label>Second URL:</Label>
            <TextBox x:Name="txtName2" />
            <Label>Third URL:</Label>
            <TextBox x:Name="txtName3" />
            <Button x:Name="btnAdd" Margin="0,5,0,0" Click="ButtonAddName_Click">Add Name</Button>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5,0,0,0" Width="auto">
            <Label>URLs to request:</Label>
        </StackPanel>

    </Grid>
</Window>
